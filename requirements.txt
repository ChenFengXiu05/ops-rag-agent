# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
httpx==0.27.0

# LangChain & LangGraph
langchain==0.2.1
langchain-community==0.2.1
langchain-openai==0.1.8
langchain-anthropic==0.1.13
langgraph==0.1.1

# Embeddings & Vector Store
chromadb==0.5.0
# sentence-transformers 依赖 torch，单独在 Dockerfile 里先装 CPU 版
sentence-transformers==3.0.0
FlagEmbedding==1.2.10

# Document Loading（去掉 unstructured，用轻量替代）
pypdf==4.2.0
python-docx==1.1.2
# markdown 文件用 TextLoader 直接读取，无需 unstructured

# Prometheus / Alertmanager
prometheus-client==0.20.0

# Elasticsearch
elasticsearch==8.13.1

# Evaluation
ragas==0.1.9
datasets==2.19.1

# Observability
langsmith==0.1.65

# Notification
requests==2.31.0

# Utilities
python-dotenv==1.0.1
tenacity==8.3.0
loguru==0.7.2
PyYAML==6.0.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
